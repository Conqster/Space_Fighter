using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// Trying to create a script to keeep player in the zone 
/// by disabling player input when player is trying yto leave the zone
/// 
/// </summary>
public class PlayerZone : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
